# learning-js

## it is a simple server client application for blogs  
### note that this is a learning project
run `npm run build-fe` to create the front-end bundle
run `npm run start-fe` to start the font-end development server
run `npm run start-be` to start the back-end server